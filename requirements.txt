# Python-level deps only.  The complete environment (MPI runtime, mpiexec,
# C++ compiler with OpenMP, cmake/ninja/pybind11) is specified in
# environment.yml — use that when deploying to a new machine.
numpy>=1.26.0
scipy>=1.11.0
numba>=0.59.0
h5py>=3.10.0
tqdm>=4.66.0
matplotlib>=3.8.0


